
type movies{
    id: Int!
    name: String!
    score: Int!
}

type Query{
    Movies : [movies]
}
type Mutation{
    delMovise(id:Int!) : [movies]!
    addMovise(name : String!,score:Int!) : [movies]
}